const textColor = '#FFFFFF';
const dFlex = 'display: flex';

const displaySize = {
  tabletM: '859px'
};

const device = {
  tabletM: `(max-width: ${displaySize.tabletM})`
};

export { textColor, dFlex, device };
